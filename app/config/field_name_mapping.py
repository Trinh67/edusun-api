data_mapping = {
    "en": {
        "RequiredField": "{{fieldName}} is required",
        "InvalidField": "{{fieldName}} is invalid",
        "InvalidJson": "Json format is invalid",
        "InvalidFieldFormat": "{{fieldName}} format is invalid",
        "InvalidMinLength": "{{fieldName}} must have at least {{limit}} characters",
        "InvalidMaxLength": "{{fieldName}} must have at most {{limit}} characters"
    },
    "vi": {
        "RequiredField": "{{fieldName}} là bắt buộc",
        "InvalidField": "{{fieldName}} không hợp lệ",
        "InvalidJson": "Định dạng json không hợp lệ",
        "InvalidFieldFormat": "{{fieldName}} format is ",
        "CoreException": "Thất bại khi gọi sang Referral Service {{api}}",
        "InvalidMinLength": "{{fieldName}} phải là chuỗi dài tối thiểu {{limit}} ký tự",
        "InvalidMaxLength": "{{fieldName}} phải là chuỗi dài tối đa {{limit}} ký tự"
    }
}